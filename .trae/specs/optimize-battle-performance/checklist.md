## Part A: 三大核心动作验证
- [ ] 进入战斗页面时播放战雾消散（rgba(15,10,5,0.6) 0.5s power2.out 淡出）
- [ ] 六边形从中心向外逐圈显现（stagger 0.04s/圈，opacity 0→1 + scale 0.92→1，back.out(1.4)）
- [ ] 棋子最后落入格位（scale 0.4→1 + y 12→0，0.4s，back.out(1.4)，stagger 0.03s）
- [ ] 现有 liftCurtain 帷幕升级行为完全不变
- [ ] 选中棋子时播放 scale 0.92→1.06→1 弹性（0.4s back.out(1.4)）
- [ ] 选中棋子下方出现金色径向光环（scale 0→1.2 + opacity 0→0.6→0.35，0.5s power2.out）
- [ ] 光环稳态后保持 sine.inOut 1.8s loop 脉冲（opacity 0.25↔0.45）
- [ ] 现有金色双描边发光完全保留，叠加在光环之上
- [ ] 攻击时攻击方棋子向目标方向前冲 8px（0.1s power2.out + 0.15s power2.in 回弹）
- [ ] 防守方位置生成金色径向冲击波（scale 0.3→2.5 + opacity 0.8→0，0.4s power2.out）
- [ ] 暴击/溃逃触发画布微震 1 帧（translate 2px 随机方向），普通攻击不震屏
- [ ] 现有伤害飘字完全保留，叠加在冲击波之上

## Part B: 配套动画验证
- [ ] 敌方棋子移动时播放滑动动画（260ms + 拖影），与玩家方一致
- [ ] 玩家方移动动画行为完全不变
- [ ] 敌方移动动画完成后才执行后续攻击（max(350, 280) 时序）
- [ ] 装备选择器关闭时有退出动画（opacity 1→0, y 0→-15, scale 1→0.96，0.2s power2.in）
- [ ] 背包关闭时有退出动画
- [ ] 关卡结算关闭时有退出动画
- [ ] 召唤结果关闭时有退出动画
- [ ] modal-overlay 有 opacity transition
- [ ] modal 退出动画完成后再 remove()，无闪烁
- [ ] 关卡结算 modal 有入场动画（overlay 0.3s 淡入 + 内容 opacity 0, scale 0.85, rotationY -10 → 1 0.5s back.out(1.4) + 卡片 stagger 0.08s）
- [ ] AI 对战结算 modal 有入场动画
- [ ] AI 单位详情 modal 有入场动画
- [ ] 商城积分变化时有计数动画（0.5s power2.out）
- [ ] 准备界面积分变化时有计数动画
- [ ] prepInvBadge 数量变化时有弹跳放大（scale 1.3→1，0.3s back.out）
- [ ] 结算 +pointsDelta 显示有计数动画
- [ ] 商城 tab 切换有 fade-in + stagger（opacity 0, y 10 → 1，0.3s power2.out，stagger 0.04s）
- [ ] 背包 tab 切换有同样过渡
- [ ] tab active 指示条有 transition: all 0.3s cubic-bezier(.34,1.56,.64,1)
- [ ] 背包物品列表有 stagger 入场
- [ ] 召唤历史列表有 stagger 入场
- [ ] 商城卡片列表有 stagger 入场
- [ ] 出售物品时有 fade-out + scale 0.8 动画（0.3s power2.in），完成后再 splice
- [ ] 棋子放置到棋盘时有 scale 0.5→1（0.3s back.out(1.4)）+ 一次性金色圆环
- [ ] 棋子回收到 bench 时有 scale 1→0.5, opacity 1→0（0.25s power2.in）
- [ ] .drag-over CSS 规则生效（高亮边框 + 微微放大）
- [ ] showToast 调用 showToastAnim，有 GSAP 入场（opacity 0, y -20, scale 0.9 → 1，0.3s back.out(1.4)）
- [ ] 召唤结果 modal 内容调用 revealCard（rotationY 90→0, scale 0.5→1, opacity 0→1，0.6s back.out(1.4)）
- [ ] 现有召唤 modal 入场 timeline 完全保留
- [ ] 锁定关卡点击有 shake 动画（translateX -5→5→-3→3→0，0.4s）

## Part C: 帧率优化验证
- [ ] 棋盘背景（六边形网格、坐标轴）已缓存到离屏 canvas，render() 中通过 drawImage 复制
- [ ] 高亮状态（sp/mv/atk/hv）的六边形仍实时叠加绘制（未错误缓存）
- [ ] 单位图像（含圆形裁剪、边框、底色）已缓存，render() 中通过 drawImage 复制
- [ ] 动画中棋子（drawAnimatingPiecesOnCanvas）也使用缓存图像
- [ ] HP 条、士气条、状态图标、朝向三角形、名称标签、选中光环、冲击波仍每帧实时绘制
- [ ] 单位图片异步加载完成后正确失效缓存并触发重绘
- [ ] computeStats 结果已缓存，选中棋子时不再每帧重算
- [ ] 装备/品阶变化时正确失效 computeStats 缓存
- [ ] startAnimLoop 持续 rAF 循环行为不变（进入战斗页运行，退出时停止）
- [ ] rAF 循环内部脏标记判断生效：无变化时跳过重绘，有变化时正常 render()
- [ ] 有移动动画、选中弹性、冲击震动等主动画期间持续渲染
- [ ] requestRender() 全局函数可用，多处调用合并为单次渲染
- [ ] mousemove handler 不再直接调用 render()，改为 requestRender()
- [ ] mousemove handler 仅在 hoveredHex 真正变化时才更新 tooltip
- [ ] AI 回合中的 render() 调用已改为 requestRender()
- [ ] combat.js 中的 render() 调用已改为 requestRender()
- [ ] 伤害飘字使用对象池，无频繁 createElement/remove
- [ ] combat-flash 元素使用对象池
- [ ] 伤害飘字动画效果（上升、缩放、淡出、暴击闪光）与原来完全一致
- [ ] ParticleBG 余烬光晕使用 sprite 预渲染，不再每帧 createRadialGradient
- [ ] ParticleBG 视觉效果与原来一致

## Part D: 回归与性能验证
- [ ] 召唤动画、页面过渡、Toast 动画等其他动画完全不变
- [ ] 选中棋子高亮、移动范围、攻击范围正确显示
- [ ] 战斗预览 tooltip 在悬停切换目标时正确更新
- [ ] 进入/退出战斗页面时动画循环正确启停（行为不变）
- [ ] 所有 modal 入场和退出动画对称且无闪烁
- [ ] 无内存泄漏（对象池元素数量稳定，缓存大小可控）
- [ ] 不引入新颜色/字体，所有新增动画使用现有调色板
- [ ] 缓动/时长统一遵循 Motion Design Language 表格
- [ ] 整体帧率提升，战斗交互明显更流畅
